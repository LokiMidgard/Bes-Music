<UserControl
    x:Class="MusicPlayer.Controls.Albums"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MusicPlayer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converters="using:MusicPlayer.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MusicPlayer.Viewmodels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">


    <GridView x:Name="toRender" ItemsSource="{x:Bind AlbumViewmodel.Albums}" ContainerContentChanging="ToRender_ContainerContentChanging" ItemClick="AlbumClicked" IsItemClickEnabled="True">
        <GridView.ItemTemplate>
            <DataTemplate x:DataType="viewmodels:AlbumViewmodel" >
                <Border Margin="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Image x:Name="cover" Width="150" Height="150"/>
                        <TextBlock Text="{x:Bind Name}" x:Name="name" Grid.Row="1" ></TextBlock>
                    </Grid>
                </Border>
            </DataTemplate>
        </GridView.ItemTemplate>
        <!--<ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" ></ItemsWrapGrid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>-->

    </GridView>
</UserControl>
