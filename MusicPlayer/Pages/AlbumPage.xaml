<Page
    x:Class="MusicPlayer.Pages.AlbumPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MusicPlayer.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converters="using:MusicPlayer.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MusicPlayer.Viewmodels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <converters:ListConverter x:Key="listConverter"/>
    </Page.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="400"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Background="{StaticResource SystemControlBackgroundBaseLowBrush}">
            <Grid Margin="100,100,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Image Width="200" Height="200" HorizontalAlignment="Left" VerticalAlignment="Top"  x:Name="cover"  />
                <StackPanel Margin="208,0,0,0" VerticalAlignment="Bottom">
                    <TextBlock Text="{Binding Name}"  Style="{StaticResource HeaderTextBlockStyle}" x:Name="name" MaxLines="1" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding Interprets, Converter={StaticResource listConverter}}" Style="{StaticResource SubheaderTextBlockStyle}" MaxLines="1" />
                </StackPanel>
            </Grid>
        </Border>
        <ListView Grid.Row="1" ItemsSource="{Binding Songs}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:SongViewmodel">
                    <StackPanel Orientation="Horizontal">
                        <Button Click="Button_Click" Tag="{x:Bind }" >
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="" ></FontIcon>
                        </Button>
                        <TextBlock Text="{x:Bind Title}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
